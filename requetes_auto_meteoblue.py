import urllib
from urllib import parse, request
import csv 
import os 
import requests

def GetUrl (lat, lon):
    url_base = "http..."
    parameters = {}
    
    #We then stock our data into a file
    x_coord = str(x)
    y_coord = str(y)
    our_file = "Data fie"